## 1. Records (익명 구조체)

기존에는 여러 값을 반환하려면 Map, List를 쓰거나 전용 class를 만들어야 했습니다. **Records**는 가볍고 익명으로 여러 타입을 묶을 수 있는 **불변(Immutable) 집합**입니다.

### 특징

- **강력한 타입 체크**: 컴파일 타임에 각 요소의 타입을 보장합니다.
- **불변성**: 생성 후 값을 변경할 수 없습니다.
- **값 기반 비교**: 내부 값이 같으면 같은 객체로 취급합니다.

```dart
// 1. 위치 기반 필드 (Positional fields)
(String, int) getUser() {
  return ('User name', 291);
}

// 2. 이름 지정 필드 (Named fields)
({String name, int age}) getUserDetail() {
  return (name: 'user name', age: 291);
}

void main() {
  final user = getUser();
  print(user.$1); // user name ($1, $2 순서로 접근)
  
  final detail = getUserDetail();
  print(detail.name); // 이름으로 직접 접근 가능
}
```

## 2. Patterns & Destructuring (패턴 매칭과 구조 분해)

패턴은 값이 특정 구조인지 확인하고, 그 구조에서 데이터를 쏙 뽑아내는(구조 분해) 기능입니다.

### 1) 변수 선언 시 구조 분해

```dart
void main() {
  var user = ('user name', 291);
  
  // Kotlin의 val (name, age) = user 와 동일
  var (name, age) = user;
  
  print('이름: $name, 나이: $age');
}
```

### 2) 복잡한 데이터(JSON) 구조 분해

실무에서 API 응답을 처리할 때 가장 강력합니다.

```dart
void handleJson(Map<String, dynamic> json) {
  // JSON의 구조를 패턴으로 검사하고 즉시 변수 할당
  if (json case {'user': [String name, int age]}) {
    print('User: $name ($age)');
  } else {
    print('Invalid JSON structure');
  }
}
```

## 3. Switch Expressions (Switch 식)

Kotlin의 when 문처럼 **값을 반환하는 Switch**입니다. 코드가 훨씬 간결해지고 **완전성(Exhaustiveness) 검사**를 지원합니다.

```dart
String getStatusMessage(int statusCode) {
  return switch (statusCode) {
    200 => 'OK',
    201 || 202 => 'Accepted', // 여러 조건 허용
    >= 400 && <= 499 => 'Client Error', // 가드(Guard) 및 범위 연산
    _ => 'Unknown', // Default (반드시 필요)
  };
}
```

## 4. 패턴과 가드(Guard - when 키워드)

패턴 매칭 도중 추가적인 조건을 걸고 싶을 때 when을 사용합니다.

```dart
void checkPoint((int, int) point) {
  switch (point) {
    case (var x, var y) when x == y:
      print('대각선 지점 ($x, $y)');
    case (var x, var y):
      print('일반 지점 ($x, $y)');
  }
}
```

## 🔍 왜 이런 문법이 도입되었을까?

### 1. 가독성 및 생산성 (Boilerplate 감소)

단순히 데이터를 묶어 반환하기 위해 매번 클래스를 정의하는 수고를 덜어줍니다.

### 2. 안전성 (Exhaustiveness checking)

**Sealed Class**와 결합하면, switch 문에서 모든 케이스를 처리했는지 컴파일러가 체크해 줍니다.

- 기존: default 문을 빼먹으면 런타임에 에러가 날 수 있음.
- Dart 3: 모든 하위 타입을 처리하지 않으면 **컴파일 에러** 발생.

### 3. 성능 (Compiler Optimization)

Records는 객체 생성 오버헤드가 매우 적고, 패턴 매칭은 컴파일러가 고도로 최적화된 점프 테이블로 변환하여 실행 속도가 빠릅니다.

# 요약

### 📦 Records

- 익명의 불변 집합 타입.
- `(int, String)`와 같은 형태로 선언하며 `$1`, `$2` 혹은 Named 파라미터로 접근.
- 전용 클래스 없이 여러 값을 안전하게 리턴할 때 최적.

### 🧩 Patterns & Destructuring

- 데이터의 모양을 확인하고 동시에 데이터를 추출하는 기법.
- 변수 선언, 할당, 반복문, Switch 문에서 활용 가능.
- 복잡한 JSON 데이터를 검증하고 파싱할 때 코드 양을 획기적으로 줄임.

### 🔄 Modern Switch

- **Switch Expression**: 값을 반환하는 형태 (`var result = switch(val) { ... }`).
- **Logical Operators**: `||`, `&&`를 패턴 내에서 사용 가능.
- **Relational Operators**: `>`, `<=`, `==` 등을 이용한 조건부 매칭.

### 🚀 실무 예제

```dart
// API 응답 처리 예시
void printUser((String, {int age}) userInfo) {
  var (name, age: age) = userInfo;
  print('$name은(는) $age살입니다.');
}
```